### These parameters would give a configuration similar to the current EPTA
### set of pulsars, to define new mock catalogs copy and modify this file

### This is specifies the priors for your signal parameters...
dt_dict:
    ### This is the delta t 
    which_distribution: "gaussian"
    mean: 5.325305761681838
    std: 0.1

T_span_dict:
    ### Add description
    which_distribution: "gaussian"
    mean: 1.0
    std: 0.0125

wn_dict:
    ### Add description
    which_distribution: "gaussian"
    mean: -40
    std: 0.01

dm_noise_log_10_A_dict:
    ### Add description
    which_distribution: "uniform"
    min: -40
    max: -39

red_noise_log_10_A_dict:
    ### Add description
    which_distribution: "uniform"
    min: -40
    max: -39

sv_noise_log_10_A_dict:
    ### Add description
    which_distribution: "uniform"
    min: -40
    max: -39

dm_noise_g_dict:
    ### Add description
    which_distribution: "uniform"
    min: 0.25
    max: 5.0

red_noise_g_dict:
    ### Add description
    which_distribution: "uniform"
    min: 0.25
    max: 5.0

sv_noise_g_dict:
    ### Add description
    which_distribution: "uniform"
    min: 0.25
    max: 5.0

noise_probabilities_dict:
    ### Add description
    ### probabilities for the different noises to be present
    red : .9
    dm : .3
    sv : 0.05